# v0.3.28 — Remove LSP/MCP Servers, Lean on Skills + Optimal CLI

> **Status:** Brainstorming complete; design approved via single-question flow.

## Goal

Remove `llmwiki-cli lsp` and `llmwiki-cli mcp` entirely. Agents discover the right CLI behavior from skills (which describe when to run `wiki config validate`, `wiki doctor`, etc.) instead of from a persistent editor protocol connection.

The CLI is a single Rust binary for managing a Karpathy-style LLM Wiki. LSP/MCP add ~1,475 lines of source + skills + tests + 2 heavy dependencies + a `tokio = full` runtime for code that an agent could trigger via a single Bash call.

## Why now

1. **Unused surface.** No automated feedback loop confirms real editor/host usage. Skills' `references/*.md` docs have no evidence of being read.
2. **Functionality already duplicated.** `wiki config validate` (v0.3.27+ unknown-key warnings), `wiki config show-schema`, and `wiki doctor` cover every LSP/MCP feature at the CLI level.
3. **Single-user reality.** Plugin manifests lag the CLI by 19 minor versions (0.3.8 vs 0.3.27) — distribution is already broken.
4. **Skill-first mental model.** Agents in Claude Code / Cursor / Codex / Kimi work best with Bash subcommand delegation. Editor connections add latency and failure modes.

## Architecture

### Before

```
~/.agents/skills/wiki/
├── LSP/              ← configures wiki lsp (Helix/Neovim/Zed/VS Code)
├── MCP/              ← configures wiki mcp (Claude Desktop/Codex/Cursor)
└── ...8 other skills

src/cli/lsp.rs        (220 lines, tower-lsp-server)
src/cli/mcp.rs        (205 lines, rmcp)
src/core/lsp_domain.rs (403 lines, shared domain)
```

### After

```
~/.agents/skills/wiki/
├── CONFIG/           ← NEW: teaches agent the validate-after-edit workflow
└── ...9 other skills (SETUP, INGEST, SEARCH, QUERY, LINT, MODELS,
                       SYNC, TROUBLESHOOTING — LSP/ and MCP/ removed)

src/cli/              ← no lsp.rs, no mcp.rs
src/core/             ← no lsp_domain.rs
```

### Decision flow

When an agent needs to edit or diagnose wiki config:

```
1. Read ~/.agents/wiki-root.toml (or per-workspace config.toml)
2. Run: llmwiki-cli config show-effective
3. (Optional) Run: llmwiki-cli config show-schema --section wiki
4. Edit the file
5. Run: llmwiki-cli config validate
6. If doctor-like diagnosis needed: llmwiki-cli doctor
```

This sequence is taught by the new `CONFIG` sub-skill and reinforced in the hub SKILL.md.

## File-by-file changes

### Removed (12 files, ~1,562 lines)

| Path | Lines | Reason |
|------|------:|--------|
| `src/cli/lsp.rs` | 220 | LSP server, stdio transport |
| `src/cli/mcp.rs` | 205 | MCP server, stdio transport |
| `src/core/lsp_domain.rs` | 403 | Only used by LSP/MCP |
| `tests/lsp_cli_test.rs` | 12 | LSP `--help` test |
| `tests/lsp_protocol_test.rs` | 38 | LSP smoke test |
| `tests/mcp_cli_test.rs` | 11 | MCP `--help` test |
| `tests/mcp_protocol_test.rs` | 33 | MCP smoke test |
| `marketplace/skills/wiki/LSP/SKILL.md` | 73 | Skill for removed server |
| `marketplace/skills/wiki/LSP/references/lsp.md` | 111 | Editor config snippets |
| `marketplace/skills/wiki/LSP/evals/evals.json` | 16 | LSP evals |
| `marketplace/skills/wiki/MCP/SKILL.md` | 87 | Skill for removed server |
| `marketplace/skills/wiki/MCP/references/mcp.md` | 105 | Host config snippets |
| `marketplace/skills/wiki/MCP/references/doctor.schema.json` | 105 | Auto-gen, no consumer |
| `marketplace/skills/wiki/MCP/evals/evals.json` | 16 | MCP evals |
| **Subtotal** | **~1,435** | |

(Plus 40 lines of `src/skills/mod.rs` entries for `lsp` / `mcp`.)

### Added (~280 lines)

| Path | Lines | Purpose |
|------|------:|---------|
| `marketplace/skills/wiki/CONFIG/SKILL.md` | ~180 | New sub-skill: edit-config workflow |
| Hub `marketplace/skills/wiki/SKILL.md` (delta) | ~+60 | "Before editing config" section |
| `src/skills/mod.rs` (delta) | +2 lines | Add CONFIG entry to TOPICS |

### Modified

| Path | Change |
|------|--------|
| `Cargo.toml` | Drop `tower-lsp-server`, `rmcp`; bump `version = "0.3.28"` |
| `Cargo.lock` | Auto-regenerated by `cargo build` |
| `src/cli/mod.rs` | Drop `pub mod lsp; pub mod mcp;`; drop `Command::Lsp`, `Command::Mcp`; drop dispatch arms |
| `src/main.rs` | No change (calls `cli::run` which calls `tokio::main`) |
| `src/lib.rs` | No change |
| `src/skills/mod.rs` | Drop `("lsp", LSP)` and `("mcp", MCP)` from `TOPICS`; add `("config", CONFIG)`; drop `include_str!` for LSP/MCP |
| `build.rs` | Drop `cargo:rerun-if-changed=marketplace/skills/wiki/LSP/SKILL.md`; drop `cargo:rerun-if-changed=marketplace/skills/wiki/MCP/SKILL.md`; drop doctor schema generation |
| `src/cli/doctor_report.rs` | No change (still used by `wiki doctor`) |
| `marketplace/skills/wiki/SETUP/SKILL.md` | Drop cross-references to `lsp/` and `mcp/` (lines 91, 98) |
| `marketplace/skills/wiki/SKILL.md` (hub) | Add "Before editing config" section (per diff) |
| `README.md` | Drop `wiki lsp` and `wiki mcp` from command list; drop LSP/MCP bullets |
| `AGENTS.md` | Drop LSP/MCP mentions in module layout, JSON schema section, registry section |
| `CHANGELOG.md` | Add v0.3.28 section |
| `tests/install_skill_test.rs` | Update: `wiki/MCP/SKILL.md` assertion → `wiki/CONFIG/SKILL.md` |
| `tests/doctor_test.rs` | Drop the `doctor.schema.json` validation (no file anymore) |
| `tests/e2e_v030_test.sh` | Drop `lsp --help` / `mcp --help` iterations; drop `lsp`/`mcp` from topics list |

### Untouched

- All other CLI commands
- All other sub-skills
- All other tests
- All wiki content
- `src/cli/doctor.rs` (still works; just doesn't write JSON schema anymore)

## New CONFIG sub-skill

`marketplace/skills/wiki/CONFIG/SKILL.md` (~180 lines):

```yaml
---
name: config
description: |
  Manage wiki configuration: edit wiki-root.toml or per-workspace
  config.toml, validate changes, diagnose config issues. Use when the
  user asks to change a wiki's pages_dir, exclude_dirs, NIM model,
  or any other setting; or when a config typo causes errors.
  Do NOT use for: installing the binary itself (use setup), editing
  wiki content (use search/query/lint).
whenToUse: |
  - "add a wiki alias"
  - "change the embed model"
  - "validate wiki-root.toml"
  - "why is my config not taking effect?"
argument-hint: "[action]"
allowed-tools: Bash(llmwiki-cli:*), Read, Edit, Glob
license: Apache-2.0
---

# Wiki — CONFIG

## Decision Router

| User says | Action |
|-----------|--------|
| "edit wiki-root.toml" | 1. `wiki config show-effective`, 2. edit file, 3. `wiki config validate` |
| "add a wiki alias" | `wiki config add <alias> <path>` (interactive) or edit TOML directly |
| "change embed model" | `wiki config set <alias> nim.embed_model <model>` or edit TOML |
| "what does my config resolve to?" | `wiki config show-effective` |
| "validate my config" | `wiki config validate` |
| "diagnose a config problem" | `wiki doctor` then `wiki config validate` |
| "what keys are available?" | `wiki config show-schema` |

## Capability Table

| Command | What you get |
|---------|--------------|
| `wiki config list` | All registered aliases + defaults |
| `wiki config paths` | Search order for config files |
| `wiki config show-effective` | Resolved config with `<source>` attribution |
| `wiki config show-schema` | JSON Schema for the Config type |
| `wiki config validate` | Structural + unknown-key + field-level checks |
| `wiki config get <key>` | Single value (dotted key path) |
| `wiki config set <alias> <key> <value>` | Atomic write |
| `wiki config add <alias> <path>` | New alias |
| `wiki config rm <alias>` | Remove alias |
| `wiki config unset <alias> <key>` | Remove a key |
| `wiki config path` | Resolved config file path |
| `wiki config edit` | Open config in `$EDITOR` |

## The edit-config workflow

For AI agents editing wiki-root.toml:

1. **Read current state**: `wiki config show-effective`
2. **Discover available keys**: `wiki config show-schema` (filter with `--section wiki|nim`)
3. **Edit the file** (use Edit tool, preserve formatting)
4. **Validate after every change**: `wiki config validate`
5. **Diagnose if something feels wrong**: `wiki doctor`

Never skip step 4 — `wiki config validate` catches:
- Unknown keys (typos like `pages_dirr`)
- Invalid TOML syntax
- Invalid model names
- Bad chunk-size combinations
- Other field-level violations

## Anti-patterns

- ❌ Do NOT edit `wiki-root.toml` and skip `wiki config validate`.
- ❌ Do NOT guess TOML key names from structure — run `wiki config show-schema`.
- ❌ Do NOT spawn the binary as a long-running server — every command is one-shot.
- ❌ Do NOT add a JSON-RPC wrapper around `wiki config validate`.

## CONTRAST

Use this skill for config editing. Use SETUP for first-run install, SEARCH/QUERY for content, LINT for hygiene.
```

## Hub SKILL.md additions

Add a "Before editing config" section after the existing "When to use `wiki` CLI" section:

```markdown
## Before editing any config

For AI agents editing wiki-root.toml or per-workspace config.toml:

1. Run `wiki config show-effective` first to see what's resolved
2. Run `wiki config show-schema` to discover available keys
3. Edit the file with the Edit tool
4. Run `wiki config validate` after every change — it catches:
   - Unknown keys (typos)
   - Invalid TOML syntax
   - Invalid model names
   - Bad chunk-size combinations
5. Run `wiki doctor` for full diagnostic if something feels off

See the `CONFIG` sub-skill for the full workflow.
```

## Tests

### Existing tests (no changes)

- `tests/config_cli_test.rs` (44 tests) — already covers all `wiki config *` subcommands
- `tests/config_test.rs` (10 tests) — core config validation
- `tests/doctor_test.rs` (5 tests, 1 modified) — `wiki doctor` validation

### Modified tests

- `tests/install_skill_test.rs` — change `MCP/SKILL.md` assertion to `CONFIG/SKILL.md`
- `tests/doctor_test.rs` — drop the JSON Schema validation subtest (no schema file)
- `tests/e2e_v030_test.sh` — drop LSP/MCP iterations

### New tests

- `tests/config_cli_test.rs::subprocess_config_show_schema_section_filters_output` — verify `--section` flag
- `tests/config_cli_test.rs::subprocess_config_validate_unknown_key_after_edit` — verify validate-after-edit workflow

## Version & CHANGELOG

### Cargo.toml

```toml
version = "0.3.28"
```

Drop:
```toml
tower-lsp-server = "0.23"
rmcp = { version = "1.7", features = ["server", "macros", "schemars", "transport-io"] }
```

### CHANGELOG.md (new section)

```markdown
## v0.3.28 — Lean CLI

**Removed — `wiki lsp` and `wiki mcp`:**
- The LSP server (`llmwiki-cli lsp`) and MCP server (`llmwiki-cli mcp`)
  are removed. The CLI is now a single-purpose file tool.
- Skills `lsp/` and `mcp/` are removed from the marketplace bundle.
- Source files removed: `src/cli/lsp.rs`, `src/cli/mcp.rs`,
  `src/core/lsp_domain.rs` (shared domain, only used by the servers).
- Dependencies removed: `tower-lsp-server`, `rmcp`.
- 4 protocol-level tests removed (`lsp_*`, `mcp_*`).
- ~1,475 lines removed across source + skills + tests + docs.

**Migration:** if you wired `llmwiki-cli lsp` into your editor (Helix,
Neovim, Zed, VS Code) or `llmwiki-cli mcp` into your AI host (Claude
Desktop, Claude Code, Cursor, Codex), remove the config block — the
subcommands no longer exist.

**Added — `wiki config show-schema --section <wiki|nim>`:**
- Filter the schema output by section. Agents writing configs no
  longer need to scroll through 200+ lines of JSON.

**Added — `CONFIG` sub-skill:**
- Teaches AI agents the edit-config workflow:
  show-effective → show-schema → edit → validate → doctor.
- Replaces the role the `lsp/` and `mcp/` skills filled.
```

## Risk assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| User had editor LSP configured | Low | Documented in CHANGELOG; no data loss |
| Existing `~/.agents/skills/wiki/LSP|SKILL.md` files stale | Low | `wiki install-skill` overwrites on next run |
| Hub SKILL.md lag vs marketplace | Pre-existing | Not addressed here (separate issue) |
| `tokio = full` in deps | None | Still needed by tests; future-proof |

## Rollback plan

The v0.3.27 binary is on the CLI; users can pin to it. No data migration needed — `wiki-root.toml` and `config.toml` are unchanged.

## Open questions

None. The brainstorming was constrained (full removal, skills-first) and the design follows directly.