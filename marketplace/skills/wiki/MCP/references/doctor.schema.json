{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DoctorReport",
  "description": "JSON Schema for the output of `wiki doctor --json`. Auto-generated\nfrom the `DoctorReport` struct below (kept in sync with\n`src/cli/doctor.rs::DoctorReport` via the\n`doctor_json_output_validates_against_schema` drift test in\n`tests/doctor_test.rs`).\n\n**Breaking v0.3.17**: `active_alias` is `string | null` (was `\"\"`\nsentinel pre-v0.3.17). v0.3.23: `nim_status` is bounded to HTTP\nrange [100, 599] (was the full uint16 range). v0.3.22: the\n`config` field is a reflective dotted-key → value map (same\nshape as `wiki config show-effective`); `config_sources` is the\nper-key source attribution (dotted-key → file-it-came-from).",
  "type": "object",
  "properties": {
    "active_alias": {
      "description": "Active wiki-root.toml alias for this workspace, if any is registered. `None`\nmeans no alias resolves to this workspace. v0.3.17+: was `\"\"` sentinel pre-v0.3.17.",
      "type": [
        "string",
        "null"
      ]
    },
    "api_key_env": {
      "description": "Name of the env var that holds the NIM API key.",
      "type": "string"
    },
    "api_key_length": {
      "description": "Resolved NIM API key length in bytes. 0 if the key env var is unset/empty.",
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "config": {
      "description": "dotted-key to value map (same shape as `wiki config show-effective`)",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "config_loaded": {
      "description": "Whether the per-workspace / per-computer config was loaded successfully.",
      "type": "boolean"
    },
    "config_sources": {
      "description": "dotted-key to source file path map (v0.3.12+)",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "embed_model": {
      "description": "Resolved NIM embedding model name from the merged effective config.",
      "type": "string"
    },
    "embed_model_available": {
      "description": "Whether the resolved `embed_model` appears in the live NIM `/v1/models` response.",
      "type": "boolean"
    },
    "nim_base_url": {
      "description": "Resolved NIM base URL (host only, no `/v1` segment).",
      "type": "string"
    },
    "nim_error": {
      "description": "Human-readable error string from the NIM probe, or `None` on success.",
      "type": [
        "string",
        "null"
      ]
    },
    "nim_reachable": {
      "description": "Whether `GET {nim_base_url}/v1/models` returned 2xx during this run.",
      "type": "boolean"
    },
    "nim_status": {
      "description": "HTTP status from the NIM probe, or `None` on network error. v0.3.23+:\nbounded to HTTP range [100, 599] (was the u16 range [0, 65535] before).",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint16",
      "maximum": 599,
      "minimum": 100
    },
    "registry_entries": {
      "description": "Count of entries in the discovered wiki-root.toml registry (0 if no registry).",
      "type": "integer",
      "format": "uint",
      "minimum": 0
    },
    "wiki_root_path": {
      "description": "Path to the resolved `wiki-root.toml`, or empty string if no registry entry matched.",
      "type": "string"
    },
    "workspace": {
      "description": "Resolved workspace directory (canonical absolute path).",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "workspace",
    "wiki_root_path",
    "registry_entries",
    "config_loaded",
    "embed_model",
    "nim_base_url",
    "api_key_length",
    "api_key_env",
    "nim_reachable",
    "embed_model_available",
    "config",
    "config_sources"
  ]
}
